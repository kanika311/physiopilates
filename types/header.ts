export interface HeaderSettingsType {
  siteLogo: string;
  headerBgColor: string;
  menuTextColor: string;
  menuHoverColor: string;
  activeMenuColor: string;
  buttonBgColor: string;
  buttonTextColor: string;
}